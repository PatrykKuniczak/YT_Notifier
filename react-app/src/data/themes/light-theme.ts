import { createTheme } from '@mui/system';

const THEME = createTheme({
    palette: {
        background: {
            primary: '#fff',
            searchBar: '#eeebeb'
        },
        color: {
            primary: '#16121E',
            placeholder: '#16121E'
        }
    }
});

export default THEME;